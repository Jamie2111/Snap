mss>=9.0.1
opencv-python>=4.9.0
pytesseract>=0.3.10
Pillow>=10.0.0
numpy>=1.26.0
networkx>=3.2
rich>=13.7.0
pywin32>=306; sys_platform == "win32"
faster-whisper>=1.0.0
rumps>=0.4.0; sys_platform == "darwin"
pywebview>=5.0
# Optional: ML vision upgrade for hero detection. Snap runs without it
# (falls back to classical CV in extractor/vision.py); installing ultralytics
# enables HeroYOLODetector for robust character localization independent of
# the in-game outline color setting.
ultralytics>=8.2.0; python_version < "3.14"
